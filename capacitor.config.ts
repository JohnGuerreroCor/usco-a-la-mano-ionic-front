import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.usco.app',
  appName: 'plantilla-ionic-web',
  webDir: 'dist/plantilla-ionic-web'
};

export default config;
